Monorepo:
- `Html2PdfApp/` Android app (on-device HTML→PDF)
- `html2pdf-server/` Optional Node.js Puppeteer converter

Quickstart (Android)
- Open `Html2PdfApp/` in Android Studio, build and run on a device/emulator.

Quickstart (Server)
- `cd html2pdf-server && npm i && npm start`